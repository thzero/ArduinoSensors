#include <debug.h>

#include "sensorBarometer.h"

float sensorBarometer::readAltitude() {
  return 0;
}

float sensorBarometer::readAltitude(atmosphereValues values) {
  return 0;
}

atmosphereValues sensorBarometer::readAtmosphere() {
  atmosphereValues values;
  return values;
}